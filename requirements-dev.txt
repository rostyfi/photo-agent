# Development-only tooling: static analysis, coverage, and pre-commit hooks.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

ruff>=0.6.0
mypy>=1.11.0
pytest-cov>=5.0.0
pre-commit>=3.8.0
